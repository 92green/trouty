describe('Provider', () => {
    it('will return a provider component', () => {});
    it('will default to using a browser history', () => {});
    it('will accept a custom history through props', () => {});
});

describe('useRoutes', () => {
    it('will throw if called outside of a component', () => {});
    it('will return the routes transition objects', () => {});
});

describe('routes', () => {
    it('will return a routes object that matches the config shape', () => {});
});
